---
interface Props {
    equipment: {
        description?: string;
        items: Array<{
            name: string;
            desc?: string;
            description?: string;
            image?: string;
            imageAlt?: string;
            iconFallback?: string;
        }>;
    };
    cityName: string;
}

const { equipment, cityName } = Astro.props;
---

<h3 class="equip-title">Alat yang Kami gunakan di lapangan</h3>
<p class="equip-sub">Kami memiliki beberapa alat untuk pekerjaan bore pile, antara lain:</p>

<div class="equip-grid">
    {equipment.items.map((item) => (
        <div class="equip-item">
            {item.image ? (
                <img class="equip-img" src={item.image} alt={item.imageAlt || item.name} loading="lazy" />
            ) : (
                <img class="equip-img" src="/imgs/logo-agung-perkasa.webp" alt={`Ikon ${item.name}`} width="80" height="80" loading="lazy" />
            )}
            <div class="equip-info">
                <div class="equip-icon-inline">
                    <img src="/imgs/logo-agung-perkasa.webp" alt={`Ikon ${item.name}`} width="24" height="24" />
                    <h4>{item.name}</h4>
                </div>
                <p>{item.desc || item.description || ''}</p>
                <span class="equip-tag">Agung Perkasa</span>
            </div>
        </div>
    ))}
</div>
